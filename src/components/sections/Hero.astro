---
// Import icons
import Icon from "../Icon.astro";

// Import fonts
import "@fontsource/poppins/700.css";
import "@fontsource/mulish/600.css";
---

<!-- Section Home / Hero -->
<section id="home" class="hero">
  <div class="container">
    <div class="content">
      <div class="hero-main">
        <div class="hero-text">
          <h1>Desarrollador Front-End &#128075;</h1>
          <p>
            Hola, Soy Henry. Me gusta crear experiencias web impresionantes y
            funcionales que mejoren la vida de los usuarios.
          </p>
          <span>
            <a
              aria-label="github"
              rel="noreferrer"
              target="_blank"
              href="https://github.com/juniidev"
            >
              <Icon icon="github-logo" size="50px" />
            </a>
          </span>
        </div>

        <div class="hero-img"></div>
      </div>
    </div>
  </div>
</section>

<!-- Styles section -->
<style>
  .hero {
    display: flex;
    flex-direction: column;
    width: 100%;
    height: 100vh;
    background-color: var(--gray-bg-color);
    position: relative;
    margin-top: 5rem;
  }
  @media (max-width: 900px) {
    .hero {
      height: auto;
      padding: 11rem 0;
    }
  }

  .hero-main {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10rem;
    position: relative;
  }
  @media (max-width: 900px) {
    .hero-main {
      flex-direction: column-reverse;
      text-align: center;
      height: auto;
      gap: 3rem;
    }
  }

  .content {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 65rem;
    gap: 10rem;
    position: relative;
  }
  @media (max-width: 900px) {
    .content {
      flex-direction: column;
      text-align: center;
      height: auto;
      gap: 3rem;
    }
  }
  @media (min-width: 1600px) {
    .content {
      margin-top: 6rem;
    }
  }
  @media (min-width: 1900px) {
    .content {
      margin-top: 10rem;
    }
  }

  .hero-text {
    display: flex;
    flex-direction: column;
    max-width: 50rem;
    position: relative;
  }
  .hero-text h1 {
    font-size: var(--hero-size);
    color: var(--black-shade);
    line-height: 1.2;
    margin-bottom: 2rem;
    margin-top: 2rem;
  }
  @media (max-width: 500px) {
    .hero-text h1 {
      font-size: 4rem;
    }
  }
  .hero-text p {
    font-size: var(--hero-text-size);
    color: var(--text-color);
    font-family: var(--text-font);
    font-weight: 500;
    line-height: 1.6;
  }
  @media (max-width: 900px) {
    .hero-text p {
      margin-bottom: 5rem;
    }
  }
  .hero-text span {
    display: flex;
    gap: 1.3rem;
    margin: 2.5rem 0;
    cursor: pointer;
  }
  .hero-text span a:hover {
    color: var(--header-color);
    transition: all 0.3s ease;
  }
  @media (max-width: 900px) {
    .hero-text span {
      justify-content: center;
      margin-top: -2rem;
      margin-bottom: 4rem;
      gap: 1.6rem;
    }
  }

  .hero-text img {
    position: absolute;
    width: 1rem;
    top: 8.7rem;
    right: 13rem;
  }
  @media (max-width: 900px) {
    .hero-text img {
      right: 3rem;
    }
  }
  @media (max-width: 500px) {
    .hero-text img {
      top: 6.4rem;
      right: 4rem;
      width: 4.5rem;
    }
  }
  @media (max-width: 400px) {
    .hero-text img {
      right: 3rem;
    }
  }
  @media (max-width: 380px) {
    .hero-text img {
      right: 3rem;
    }
  }
  @media (max-width: 375px) {
    .hero-text img {
      right: 1.7rem;
    }
  }

  .hero-img {
    background-image: url("../../images/person1.jpeg");
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    width: 35rem;
    height: 45rem;
    position: relative;

    border-radius: 2rem;
  }
  @media (max-width: 500px) {
    .hero-img {
      width: 28rem;
      height: 28rem;
    }
  }
</style>
